spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/mood_trip_test   # 너의 테스트 DB
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate.ddl-auto: create-drop   # 테스트 동안만 생성/삭제
    show-sql: true
    properties:
      hibernate.format_sql: true